# Заполняем строки данными и считаем ширину колонок
            for m in members:
                # Универсальная проверка: если данные пришли в виде словаря — превращаем в список
                if isinstance(m, dict):
                    row_data = [
                        m.get("name", ""), m.get("login", ""), m.get("title", ""), 
                        m.get("status", ""), m.get("mail", ""), m.get("block", ""),
                        m.get("ext1", ""), m.get("ext2", ""), m.get("ext3", ""), 
                        m.get("ext4", ""), m.get("ext5", ""), m.get("ext6", "")
                    ]
                else:
                    row_data = m

                ws.append(row_data)
                
                # Расчет автоширины
                for idx, val in enumerate(row_data):
                    if val:
                        val_len = len(str(val))
                        if val_len > col_widths[idx]: 
                            col_widths[idx] = val_len
